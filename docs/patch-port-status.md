# 1.23.3 Patch 迁移清单

基准：tag 1.23.3，commit 0f2e66b56c20ddd2caeb92df2daddc354b3cca40；r2049 patches.json 共 **91** 项。

首轮宿主按用户提供的 APK 改为粉版 tv.danmaku.bili 8.27.0（8270400）。首个可见功能为底栏过滤；v4 继续移植同类 Json 子功能，范围见 [v4 报告](host-only-v4-report.md)，不代表整个 Json Patch 完成。

当前 v5 继续保留宿主内嵌设置，新增播放器功能适配，范围和限制见 [v5 报告](player-port-v5-report.md)。自动翻译按用户要求暂缓。未移植控件显示“未移植”；配置保存在宿主，旧 Remote Preferences 仅供一次迁移。用户已确认 v3 生效；v5 新功能仍待真机验证。

这是逐项源码索引与初步分级，不把静态扫描当运行验证。A/B/C 是工作量预判；before/after/replace 取决于实际控制流，不能从出现 invoke 就断言可移植。所有非首轮项目保留为待分析，完整指纹和依赖见 patch-inventory.json。

| Patch 名称 | 源文件 | 作用目标/指纹 | 修改类型 | integrations 调用 | before | after | replace | 资源处理 | 分类/实现 | 测试状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Export all activities | [ExportAllActivitiesPatch.kt](../patches/src/main/kotlin/app/revanced/patches/all/activity/exportall/ExportAllActivitiesPatch.kt) | 见源码/依赖 Patch | resource/manifest | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 是 | C / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Predictive back gesture | [PredictiveBackGesturePatch.kt](../patches/src/main/kotlin/app/revanced/patches/all/interaction/gestures/PredictiveBackGesturePatch.kt) | 见源码/依赖 Patch | resource/manifest | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 是 | C / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Enable Android debugging | [EnableAndroidDebuggingPatch.kt](../patches/src/main/kotlin/app/revanced/patches/all/misc/debugging/EnableAndroidDebuggingPatch.kt) | 见源码/依赖 Patch | resource/manifest | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 是 | C / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Change package name | [ChangePackageNamePatch.kt](../patches/src/main/kotlin/app/revanced/patches/all/misc/packagename/ChangePackageNamePatch.kt) | 见源码/依赖 Patch | resource/manifest | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 是 | C / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Spoof SIM country | [SpoofSimCountryPatch.kt](../patches/src/main/kotlin/app/revanced/patches/all/telephony/sim/spoof/SpoofSimCountryPatch.kt) | 见源码/依赖 Patch | 依赖/组合 | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Block up recommend ads | [UpRcmdAdsPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/ads/patch/UpRcmdAdsPatch.kt) | 见源码/依赖 Patch | method | patches/SettingsTransfer;->blockUpRcmdAds | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Hide follow button | [HideFollowButtonPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/layout/patch/HideFollowButtonPatch.kt) | 见源码/依赖 Patch | method | patches/SettingsTransfer;->shouldShowCommentFollow<br>patches/SettingsTransfer;->shouldShowDynamicFollow | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Remove vip section | [RemoveVipSectionPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/layout/patch/RemoveVipSectionPatch.kt) | Lcom/bilibili/lib/ui/garb/Garb;<br>Ltv/danmaku/bili/ui/main2/mine/HomeUserCenterFragment; | method | patches/RemoveVipSectionPatch;->onUserCenterFragmentResume<br>patches/RemoveVipSectionPatch;->onUserCenterFragmentSkinChanged | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| App icon | [AppIconPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/appicon/AppIconPatch.kt) | 见源码/依赖 Patch | resource | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 是 | C / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Config | [ConfigPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/config/patch/ConfigPatch.kt) | ABSourceFingerprint<br>ConfigSourceFingerprint<br>DDContractImplFingerprint | method | patches/ConfigPatch;->getAb<br>patches/ConfigPatch;->getConfig | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / v5 部分适配，见报告 | v5 静态检查通过；待真机验证 |
| Copy enhance | [CopyEnhancePatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/copy/patch/CopyEnhancePatch.kt) | Lcom/bilibili/bplus/im/business/model/BaseTypedMessage; | method | patches/CopyEnhancePatch;->onCommentLongClick<br>patches/CopyEnhancePatch;->onCommentMenuItemClick<br>patches/CopyEnhancePatch;->onConversationCopy<br>patches/CopyEnhancePatch;->onCopyDesc<br>patches/CopyEnhancePatch;->onDynamicLongClick | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | C / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Dark switch | [DarkSwitchPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/darkswitch/patch/DarkSwitchPatch.kt) | HdNewSwitchDarkModeFingerprint<br>MethodFingerprint<br>SwitchDarkModeFingerprint | method | patches/DarkSwitchPatch;->switchDarkMode | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | C / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Drawer | [DrawerPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/drawer/patch/DrawerPatch.kt) | Ltv/danmaku/bili/ui/main2/basic/BaseMainFrameFragment; | method | patches/drawer/DrawerPatch;->onMainFrameFragmentViewCreated | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Blkv | [BLKVPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/integrations/patch/BLKVPatch.kt) | BLKVFingerprint | method | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Brotli | [BrotliPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/integrations/patch/BrotliPatch.kt) | BrotliFingerprint | method | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Config integration | [ConfigPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/integrations/patch/ConfigPatch.kt) | BaseMainFrameFragmentFingerprint<br>DanmakuFontSwitchPreferenceFingerprint | method | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Dpi | [DpiPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/integrations/patch/DpiPatch.kt) | Lcom/bilibili/cron/ViewCoordinator; | method/native（需人工确认） | patches/DpiPatch;->onCronSetDisplayDensity<br>patches/main/ApplicationDelegate;->getAppResources<br>patches/main/ApplicationDelegate;->onActivityPreAttachBaseContext<br>patches/main/ApplicationDelegate;->onActivityPreConfigurationChanged | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | C / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Integrations | [IntegrationsPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/integrations/patch/IntegrationsPatch.kt) | Lcom/bilibili/app/preferences/custom/DanmakuFontSwitchPreference;<br>Lcom/bilibili/gripper/BiliApp; | method | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Lib bili | [LibBiliPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/integrations/patch/LibBiliPatch.kt) | Lcom/bilibili/nativelibrary/LibBili; | method/native（需人工确认） | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | C / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Main activity patch | [MainActivityPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/integrations/patch/MainActivityPatch.kt) | Ltv/danmaku/bili/MainActivityV2; | method | patches/main/MainActivityDelegate;->onBackPressed<br>patches/main/MainActivityDelegate;->onCreate<br>patches/main/MainActivityDelegate;->onSaveInstanceState<br>patches/main/MainActivityDelegate;->onStart | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Modify modifier | [ModifyModifierPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/integrations/patch/ModifyModifierPatch.kt) | Ltv/danmaku/bili/ui/main2/resource/MainResourceManager\$Tab;<br>Ltv/danmaku/bili/ui/main2/resource/MainResourceManager\$TabData;<br>Ltv/danmaku/bili/ui/main2/resource/MainResourceManager\$TabResponse; | method | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Anti RecyclerView obfuscation | [NormalizeRecyclerViewPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/integrations/patch/NormalizeRecyclerViewPatch.kt) | RecyclerViewHolderFingerprint | method | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| ProtoBuf print | [ProtoBufPrintPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/integrations/patch/ProtoBufPrintPatch.kt) | 见源码/依赖 Patch | method | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Json | [JSONPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/json/patch/JSONPatch.kt) | JSONFingerprint | method | patches/json/JSONPatch;->parseArrayHook<br>patches/json/JSONPatch;->parseObjectHook | 不需要 | 3 个 parseObject 返回后处理，嵌套只处理最外层；首页、我的、空间、开屏和直播等部分分支 | 部分广告/弹窗响应置空；不替换原解析方法 | 仅宿主内设置，原版目录数据，不注入宿主资源 | B / 部分移植，见 v4 报告；其他 Json 分支仍待移植 | v3 底栏用户确认；v4 构建和 26 项 JVM 检查通过，32 类/74 成员静态核对；新增真机待测 |
| Pegasus hook | [PegasusPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/json/patch/PegasusPatch.kt) | Lcom/bilibili/app/comm/list/common/data/DislikeReason;<br>Lcom/bilibili/okretro/GeneralResponse;<br>Lcom/bilibili/pegasus/api/model/BasicIndexItem; | method | patches/json/PegasusPatch;->onFeedClick<br>patches/json/PegasusPatch;->pegasusHook | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Override certificate pinning | [OverrideCertificatePinningPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/network/OverrideCertificatePinningPatch.kt) | 见源码/依赖 Patch | resource/manifest | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 是 | C / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Music notification | [MusicNotificationPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/notification/patch/MusicNotificationPatch.kt) | HeadsetMediaSessionCallbackFingerprint<br>LiveNotificationHelperFingerprint<br>MediaSessionCallbackApi21Fingerprint<br>NotificationStyleAbFingerprint | method | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | C / 未移植 | 仅静态盘点；手机验证由用户执行 |
| OkHttp | [OkHttpPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/okhttp/patch/OkHttpPatch.kt) | BodyWrapperFingerprint<br>BufferFingerprint<br>HeadersFingerprint<br>HttpUrlFingerprint<br>MediaTypeGetFingerprint<br>RealCallFingerprint<br>RequestFingerprint<br>ResponseBodyFingerprint<br>ResponseFingerprint | method | patches/okhttp/OkHttpPatch;->hook<br>patches/okhttp/OkHttpPatch;->hookBefore<br>patches/okhttp/OkHttpPatch;->shouldHook | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / v5 部分适配，见报告 | v5 静态检查通过；待真机验证 |
| Search type | [AppendExtraSearchTypePatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/AppendExtraSearchTypePatch.kt) | Lcom/bilibili/search/result/pages/BiliMainSearchResultPage\$PageTypes;<br>Lcom/bilibili/search2/result/pages/BiliMainSearchResultPage\$PageTypes; | method | patches/okhttp/BangumiSeasonHook;->onSearchResultFragmentVisible | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Append signature info for LSPatch | [AppendSignatureInfoPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/AppendSignatureInfoPatch.kt) | 见源码/依赖 Patch | resource/manifest | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 是 | C / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Auto like | [AutoLikePatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/AutoLikePatch.kt) | SectionFingerprint | method | patches/AutoLikePatch;->autoLike | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| BL route intercept | [BLRoutePatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/BLRoutePatch.kt) | BLRouteBuilderFingerprint<br>RouteRequestFingerprint | method | patches/BLRoutePatch;->intercept | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| BLog | [BLogPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/BLogPatch.kt) | LogcatAdapterFingerprint | method | patches/SettingsTransfer;->debuggable | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Bili adjust layout | [BiliLayoutAdjustPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/BiliLayoutAdjustPatch.kt) | SetUnitedTabLayoutFingerprint | method | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Bili library patch | [BiliLibraryPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/BiliLibraryPatch.kt) | 见源码/依赖 Patch | resource/native（需人工确认） | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 是 | C / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Fix app setting preference layout | [BiliSettingPreferenceLayoutPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/BiliSettingPreferenceLayoutPatch.kt) | 见源码/依赖 Patch | resource | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 是 | C / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Block thai comment | [BlockThaiCommentPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/BlockThaiCommentPatch.kt) | Lcom/bilibili/bangumi/ui/page/detail/BangumiCommentInvalidFragmentV2;<br>Lcom/bilibili/bangumi/ui/page/detail/OGVCommentFragment; | method | patches/okhttp/BangumiSeasonHook;->onCommentInvalidFragmentViewCreated | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Cache redirect | [CacheRedirectPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/CacheRedirectPatch.kt) | OnOgvDownloadFingerprint<br>TheseusOnOgvDownloadFingerprint<br>UniteDownloadMenuInvokeFingerprint | method | patches/CacheRedirectPatch;->onMenuClick<br>patches/CacheRedirectPatch;->onOgvDownload<br>patches/CacheRedirectPatch;->onTheseusOgvDownload<br>patches/CacheRedirectPatch;->onUniteDownloadMenuClick | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 是 | C / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Channel model fix | [ChannelModelFixPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/ChannelModelFixPatch.kt) | 见源码/依赖 Patch | method | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Crash handler | [CrashHandlerPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/CrashHandlerPatch.kt) | 见源码/依赖 Patch | method | patches/CrashHandlerPatch;->onCrash | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Custom splash | [CustomSplashPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/CustomSplashPatch.kt) | Ltv/danmaku/bili/ui/splash/brand/model/BrandShowInfo;<br>Ltv/danmaku/bili/ui/splash/brand/ui/BaseBrandSplashFragment; | method | patches/SplashPatch;->getMode<br>patches/SplashPatch;->onBrandSplashFragmentViewCreated | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Fake not in multi window mode | [FakeNotInMultiWindowPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/FakeNotInMultiWindowPatch.kt) | Lcom/bilibili/bangumi/ui/page/detail/BangumiDetailActivityV3;<br>Lcom/bilibili/bililive/room/ui/roomv3/LiveRoomActivityV3;<br>Lcom/bilibili/multitypeplayerV2/MultiTypeVideoContentActivity;<br>Lcom/bilibili/ship/theseus/detail/UnitedBizDetailsActivity;<br>Lcom/bilibili/ship/theseus/playlist/UnitedPlaylistActivity;<br>Lcom/bilibili/video/videodetail/VideoDetailsActivity; | method | patches/SettingsTransfer;->fakeNotInMultiWindow | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | C / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Forbid auto subscribe | [FavFolderDialogPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/FavFolderDialogPatch.kt) | FavFolderOnDataSuccessFingerprint<br>FavFolderOnDataSuccessNewFingerprint | method | patches/SettingsTransfer;->shouldAutoSubscribe | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Forbid live room auto float | [ForbidLiveRoomAutoFloatPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/ForbidLiveRoomAutoFloatPatch.kt) | LiveRoomSetFloatWindowFingerprint | method | patches/LiveRoomPatch;->disableAutoFloat | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Forbid live room slide left | [ForbidLiveRoomSlideLeftPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/ForbidLiveRoomSlideLeftPatch.kt) | LiveRoomTouchDispatchViewModelFingerprint | method | patches/LiveRoomPatch;->disableSlideLeft | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Forbid switch live room | [ForbidSwitchLiveRoomPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/ForbidSwitchLiveRoomPatch.kt) | Lcom/bilibili/bililive/room/ui/roomv3/vertical/widget/LiveVerticalPagerView; | method | patches/LiveRoomPatch;->forbidSwitchLiveRoom | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Force comment time navigable | [ForceCommentNavigablePatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/ForceCommentNavigablePatch.kt) | CommentConfigFingerprint | method | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Inject DocumentsProvider | [InjectDataProviderPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/InjectDataProviderPatch.kt) | 见源码/依赖 Patch | resource/manifest | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 是 | C / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Inject more scheme | [InjectMoreSchemePatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/InjectMoreSchemePatch.kt) | 见源码/依赖 Patch | resource/manifest | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 是 | C / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Keep data when uninstall | [KeepDataWhenUninstallPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/KeepDataWhenUninstallPatch.kt) | 见源码/依赖 Patch | resource/manifest | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 是 | C / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Disable live room double click | [LiveRoomPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/LiveRoomPatch.kt) | Lcom/bilibili/bililive/room/ui/roomv3/player/container/LiveRoomPlayerContainerView; | method | patches/LiveRoomPatch;->disableLiveRoomDoubleClick<br>patches/LiveRoomPatch;->onDoubleTap | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Make text selectable | [MakeTextSelectablePatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/MakeTextSelectablePatch.kt) | 见源码/依赖 Patch | resource | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 是 | C / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Prevent pre download module | [ModulePreDownloadHookPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/ModulePreDownloadHookPatch.kt) | ConfigV3PreloadFingerprint<br>WebConfigFingerprint | method | patches/SettingsTransfer;->delayDownloadModules | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Remove managerSpaceActivity | [NoManageSpaceActivityPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/NoManageSpaceActivityPatch.kt) | 见源码/依赖 Patch | resource/manifest | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 是 | C / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Notification icon | [NotificationPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/NotificationPatch.kt) | 见源码/依赖 Patch | 依赖/组合 | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Number format | [NumberFormatPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/NumberFormatPatch.kt) | MineBindAccountStateFingerprint<br>SpaceBindAccountStateFingerprint | method | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Per app language | [PerAppLanguagePatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/PerAppLanguagePatch.kt) | 见源码/依赖 Patch | resource/manifest | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 是 | C / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Play manifest compatibility | [PlayManifestCompatibilityPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/PlayManifestCompatibilityPatch.kt) | 见源码/依赖 Patch | resource/manifest | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 是 | C / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Disable auto select | [PublishToFollowingPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/PublishToFollowingPatch.kt) | PublishToFollowingConfigFingerprint | method | patches/SettingsTransfer;->shouldAutoSelectOnce | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Save comment images | [SaveCommentImagePatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/SaveCommentImagePatch.kt) | Lcom/bilibili/lib/imageviewer/fragment/ImageFragment; | method | patches/CommentImagePatch;->bindClickListener<br>patches/CommentImagePatch;->onCommentImageClick | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Share | [SharePatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/SharePatch.kt) | AppendTrackingInfoFingerprint | method | patches/SettingsTransfer;->disableAppendTrackingInfo | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Teenager mode | [TeenagerModePatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/TeenagerModePatch.kt) | TeenagersModeFingerprint | method | patches/SettingsTransfer;->disableTeenagerDialog | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Text fold | [TextFoldPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/TextFoldPatch.kt) | Lcom/bilibili/bplus/followingcard/widget/EllipsizingTextView; | method | patches/TextFoldPatch;->getCommentMaxLines<br>patches/TextFoldPatch;->getDynLinesToAll<br>patches/TextFoldPatch;->getDynMaxLines | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Trial quality | [TrialQualityPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/TrialQualityPatch.kt) | QualityViewHolderFingerprint | method | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Uid copy no prefix | [UidCopyNoPrefixPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/UidCopyNoPrefixPatch.kt) | UidCopyFingerprint | method | patches/SettingsTransfer;->uidCopyNoPrefix | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Upgrade | [UpgradePatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/other/patch/UpgradePatch.kt) | AttachChannelInfoFingerprint<br>MainCommonServiceImplFingerprint | method | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Comment word search url | [CommentReplyUrlPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/protobuf/patch/CommentReplyUrlPatch.kt) | 见源码/依赖 Patch | method | patches/CommentReplyUrlPatch;->filterUrls | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | B / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Moss | [MossPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/protobuf/patch/MossPatch.kt) | Lcom/bilibili/lib/moss/api/MossException;<br>Lcom/bilibili/lib/moss/api/MossResponseHandler; | method | meta/HookFlags;->STOP_EXECUTION<br>patches/protobuf/MossPatch;->hookAsyncBefore<br>patches/protobuf/MossPatch;->hookBeforeRequest<br>patches/protobuf/MossPatch;->hookBlockingAfter<br>patches/protobuf/MossPatch;->hookBlockingBefore | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | B / v5 部分适配，见报告 | v5 静态检查通过；待真机验证 |
| Unlock ProtoBuf | [UnlockProtobufPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/protobuf/patch/UnlockProtobufPatch.kt) | 见源码/依赖 Patch | method | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | B / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Fix preference manager | [FixPreferenceManagerPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/settings/patch/FixPreferenceManagerPatch.kt) | PreferenceManagerFingerprint | method | widget/CheckBoxGroupPreference;->onAttachedToHierarchy | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Fix preference click | [HdPreferenceClickFixPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/settings/patch/HdPreferenceClickFixPatch.kt) | HdOnContactClickFingerprint<br>HdOnCourseClickFingerprint<br>HdOnImClickFingerprint<br>HdOnSettingsClickFingerprint<br>HdOnWatchLaterClickFingerprint<br>MethodFingerprint | method | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Hd preference fragment | [HdPreferenceFragmentPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/settings/patch/HdPreferenceFragmentPatch.kt) | Ltv/danmaku/bilibilihd/ui/main/preference/HdPreferenceFragment; | method | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| BiliRoamingX settings entrance | [SettingsResourcePatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/settings/patch/SettingsResourcePatch.kt) | BiliPreferencesFragment.onCreatePreferences | 原 resource；现运行时 Preference | SettingsEntranceHook / SettingsScreen | 无 | 原方法返回后插入唯一 key 的 Preference | 否；原方法一次 | 新实现不修改资源表；宿主全屏系统 Dialog | B / v3 仅宿主 UI 与本地配置，非原 Fragment 全量迁移 | 入口显示已由 v2 真机确认；v3 编辑6项/迁移4项测试通过；本地目录显示与保存真机待验 |
| Splash | [SplashPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/splash/SplashPatch.kt) | 见源码/依赖 Patch | resource | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 是 | C / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Custom theme color | [CustomThemePatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/theme/patch/CustomThemePatch.kt) | BuiltInThemesFingerprint<br>SkinListFingerprint<br>ThemeClickFingerprint<br>ThemeColorsFingerprint<br>ThemeHelperFingerprint<br>ThemeNameFingerprint<br>ThemeProcessorFingerprint<br>WebActivityBuildUriFingerprint | method | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | C / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Toast | [ToastPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/toast/patch/ToastPatch.kt) | Ltv/danmaku/bili/MainActivityV2; | method | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Webview | [WebViewPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/misc/webview/patch/WebViewPatch.kt) | 见源码/依赖 Patch | 依赖/组合 | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | A / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Add back danmaku system font | [AddBackDmSystemFontPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/video/player/patch/AddBackDmSystemFontPatch.kt) | 见源码/依赖 Patch | resource | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 是 | C / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Enable Key mapping | [BangumiKeyDownHandlerPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/video/player/patch/BangumiKeyDownHandlerPatch.kt) | BangumiKeyDownHandlerFingerprint | method | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | B / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Default playback speed | [DefaultPlaybackSpeedPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/video/player/patch/DefaultPlaybackSpeedPatch.kt) | Ltv/danmaku/ijk/media/player/IMediaPlayer; | method | patches/PlaybackSpeedPatch;->defaultSpeed | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 是 | C / v5 部分适配，见报告 | v5 静态检查通过；待真机验证 |
| Force hardware codec | [HwCodecPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/video/player/patch/HwCodecPatch.kt) | Ltv/danmaku/ijk/media/player/IIjkMediaPlayerItem;<br>Ltv/danmaku/ijk/media/player/IjkMediaConfigParams; | method | patches/HwCodecPatch;->enableHwCodec<br>patches/HwCodecPatch;->printOptionBundle | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | B / v5 部分适配，见报告 | v5 静态检查通过；待真机验证 |
| Long press playback speed | [LongPressPlaybackSpeedPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/video/player/patch/LongPressPlaybackSpeedPatch.kt) | TripleSpeedServiceFingerprint<br>TripleSpeedServiceUniteFingerprint | method | patches/PlaybackSpeedPatch;->longPressSpeed | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | B / v5 部分适配，见报告 | v5 静态检查通过；待真机验证 |
| Override playback speed | [OverridePlaybackSpeedPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/video/player/patch/OverridePlaybackSpeedPatch.kt) | PlaybackSpeedSettingFingerprint<br>PlayerSpeedWidgetFingerprint<br>UnitePlayerSetSpeedMenuFingerprint | method | ") \|\| it.startsWith("Lkofua/") }         }.flatMap { c ->             c.methods.mapNotNull { m ->                 val payload = m.implementation?.instructions?.find { inst ->                     inst is ArrayPayload && inst.arrayElements.let { it == speedBytesList \|\| it == speedBytesReversedList }                 } as? DexBackedArrayPayload                 if (payload != null) {                     val (index, inst) = m.implementation?.instructions!!.withIndex().firstNotNullOf { (index, inst) ->                         if (inst.opcode == Opcode.FILL_ARRAY_DATA && inst is DexBackedInstruction31t                             && inst.let { it.instructionStart + it.codeOffset * 2 } == payload.instructionStart                         ) index to inst else null                     }                     val clazz = c.proxy(context)                     val method = clazz.methods.first { it == m }                     Info(clazz, method, index, inst.registerA)                 } else null             }         }.ifEmpty {             throw PatchException("Retrieve speed array info failed")         }.forEach { (clazz, method, index, register) ->             val instructions = method.implementation!!.instructions             val payloadInst = instructions.first { inst ->                 inst is ArrayPayload && inst.arrayElements.let { it == speedBytesList \|\| it == speedBytesReversedList }             } as ArrayPayload             val reverse = payloadInst.arrayElements == speedBytesReversedList             val patchMethodName = if (reverse) "getOverrideReverseSpeedArray" else "getOverrideSpeedArray"             method.addInstructions(                 index + 1, """                 invoke-static {v$register}, patches/PlaybackSpeedPatch;->$patchMethodName<br>patches/PlaybackSpeedPatch;->getOverrideSpeedArraySize<br>patches/PlaybackSpeedPatch;->onNewPlaybackSpeedSetting<br>patches/PlaybackSpeedPatch;->onPlaybackSpeedSelected<br>patches/PlaybackSpeedPatch;->onUnitePlayerSetSpeedMenu | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | B / v5 部分适配，见报告 | v5 静态检查通过；待真机验证 |
| Play url | [PlayUrlPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/video/player/patch/PlayUrlPatch.kt) | Ltv/danmaku/ijk/media/player/IjkMediaAsset\$MediaAssertSegment;<br>Ltv/danmaku/ijk/media/player/IjkMediaAsset\$MediaAssertSegment\$Builder; | method | patches/PlayUrlPatch;->onBuildMediaAssetSegment | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | B / v5 部分适配，见报告 | v5 静态检查通过；待真机验证 |
| Player gesture detector hook | [PlayerGestureDetectorPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/video/player/patch/PlayerGestureDetectorPatch.kt) | Ltv/danmaku/videoplayer/core/videoview/AspectRatio; | method | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | B / v5 部分适配，见报告 | v5 静态检查通过；待真机验证 |
| Player core | [PlayerPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/video/player/patch/PlayerPatch.kt) | PlayerSeekToFingerprint<br>RemoteServiceHandlerOnStartFingerprint | method | 无直接调用（可能通过依赖） | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | B / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Player toast | [PlayerToastPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/video/player/patch/PlayerToastPatch.kt) | Ltv/danmaku/biliplayerv2/widget/toast/PlayerToast; | method | patches/PlayerToastPatch;->onShow | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | B / 未移植 | 仅静态盘点；手机验证由用户执行 |
| Remember playback speed | [RememberPlaybackSpeedPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/video/player/patch/RememberPlaybackSpeedPatch.kt) | MenuFuncSegmentFingerprint<br>MenuServiceCreateSpeedFingerprint<br>PlaybackSpeedSettingFingerprint<br>PlayerSettingCreateSpeedFingerprint<br>PlayerSpeedChooseWidgetFingerprint | method | patches/PlaybackSpeedPatch;->onPlaybackSpeedSelected | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 是 | C / v5 部分适配，见报告 | v5 静态检查通过；待真机验证 |
| Video default quality | [VideoQualityPatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/video/quality/patch/VideoQualityPatch.kt) | PlayerSettingHelperFingerprint | method | patches/VideoQualityPatch;->getMatchedFullScreenQuality | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | B / v5 部分适配，见报告 | v5 静态检查通过；待真机验证 |
| Subtitle import and save | [SubtitleImportSavePatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/video/subtitle/patch/SubtitleImportSavePatch.kt) | Ltv/danmaku/biliplayerv2/injection/InjectPlayerService; | method | patches/SubtitleImportSavePatch;->onCreateSubtitleWidget | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | C / v5 部分适配，见报告 | v5 静态检查通过；待真机验证 |
| Subtitle style | [SubtitlePatch.kt](../patches/src/main/kotlin/app/revanced/patches/bilibili/video/subtitle/patch/SubtitlePatch.kt) | Lcom/bilibili/cron/Canvas;<br>Ltv/danmaku/biliplayerv2/service/interact/biz/chronos/chronosrpc/methods/receive/GetDanmakuConfig\$SubtitleConfig;<br>Ltv/danmaku/biliplayerv2/service/interact/biz/chronos/chronosrpc/methods/send/DanmakuConfigChange\$SubtitleConfig; | method | patches/SubtitlePatch;->drawPath<br>patches/SubtitlePatch;->drawText<br>patches/SubtitlePatch;->measureTextImpl<br>patches/SubtitlePatch;->offset<br>patches/SubtitlePatch;->setPaint | 待按控制流人工确认 | 待按返回路径确认 | 需看注入位置；不等同整方法替换 | 未发现直接资源操作 | B / v5 部分适配，见报告 | v5 静态检查通过；待真机验证 |
